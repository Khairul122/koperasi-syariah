<?php

// Base URL for links and assets
define('BASE_URL', 'http://localhost/synectra');

// Base Path for internal includes
define('BASE_PATH', dirname(__DIR__) . DIRECTORY_SEPARATOR);

// DB Connection constants (could also move these from koneksi.php eventually)
// For now, we'll keep koneksi.php as it is but ensure it uses the right paths if needed.
